// Typescript
// npm install -g typescript

// tsc --init

// Plugin: Code Runner
// npm i -g ts-node
// ctrl + alt + n

// compilar codigo TS para JS -> tsc index.ts

console.log("npm install -g typescript");

const pedido = (pedido: string) => {
return `Deu certo o pedido ${pedido}`;
};

console.log(pedido("1234"));

